---
language: ruby
notifications:
  email: false
branches:
  only:
  - master
os: osx
matrix:
  include:
  - env: FORMULA=build_formula
    osx_image: xcode8.2
script:
- ruby tests/build_formula.rb $FORMULA
